rules:
  - name: test-rule
    enabled: true
    watch:
      path: C:\Users\xq151\Downloads
      pattern: '*'
    events: [add, change]
    debounce: 500
    conditions:
      minSizeBytes: 0
      ignoreInitial: true
    onError: stop
    actions:
      - parallel:
          - type: simple-log
